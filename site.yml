---
- hosts: 127.0.0.1

  tasks:
    - debug: var=secret
    - debug: var=project
    - debug: var=vm
    - debug: var=vms
    - debug: var=tmp
    - debug: var=cluster_meta
    
    - name: commands
      shell: |
        hostname && whoami

    - name: write tmp/zalupa
      tmp:
        write:
          key: "key"
          value: "value"
